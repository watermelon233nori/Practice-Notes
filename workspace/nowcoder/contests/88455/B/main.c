#include <stdio.h>
#define i64 long long

int main() {
    int t;
    for (int i = 0; i < t; ++i) {
        int oper = 0;
        i64 n;
        scanf("%lld", &n);
        
        printf("%lld\n", n);
    }
    return 0;
}