#include <bits/stdc++.h>

using namespace  std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    using ul = size_t;
    ul t;
    cin >> t;
    string s;
    s.reserve(20);
    while (t--) {
        ul n; cin >> n >> s;
    }
}