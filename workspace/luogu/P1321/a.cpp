#include <bits/stdc++.h>

using namespace std;

char boy[] = "yob", girl[] = "lrig";

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string str;
    str.reserve(300);
    cin >> str;
    return 0;
}