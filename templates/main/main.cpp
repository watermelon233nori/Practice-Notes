#include <bits/stdc++.h>
#include <algorithm>

int main() {
    /* Input & output optimization refers to https://oi-wiki.org/contest/io/ */
    // Disable the synchronization with C standard input
    std::ios::sync_with_stdio(false);
    // Disable the tie between std::cin and std::cout
    std::cin.tie(nullptr);

    return 0;
}