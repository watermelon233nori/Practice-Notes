#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    long double d;
    cin >> d;
    cout << fixed << setprecision(16) << ((d/2) * (d/2) * M_PI) << '\n';
}