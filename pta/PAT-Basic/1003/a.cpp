#include <iostream>
#include <string>
#include <cstdlib>
#pragma GCC optimize(2)
#define IOS std::ios::sync_with_stdio(false);std::cin.tie(nullptr);
using namespace std;

int main() {
    IOS;
    
    return 0;
}