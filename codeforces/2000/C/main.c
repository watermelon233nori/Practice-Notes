#include <stdio.h>

int main() {
    int t;
    scanf("%d", &t);
    for (int i = 0; i < t; ++i) {
        int m, n;
        scanf("%d", &n);
        int a[n];
        for (int j = 0; j < n; ++j) {
            scanf("%d", &(a[j]));
        }
        scanf("%d", &m);
    }
}