#include <bits/stdc++.h>

using namespace std;
#define el cout<<'\n';
unsigned short nums[(int)1e6];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin >> n;
    for (int i = 0; i < n; ++i) {
        cin >> nums[i];
    }
    for (int i = n - 1; i >= 1; --i) {
        
    }
    el;
    return 0;
}