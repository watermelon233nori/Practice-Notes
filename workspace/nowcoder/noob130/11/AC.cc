#include <cmath>
#include <iomanip>
#include <iostream>

using namespace std;

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    long double n;
    cin >> n;
    cout << (int)sqrt(n) << '\n';
}