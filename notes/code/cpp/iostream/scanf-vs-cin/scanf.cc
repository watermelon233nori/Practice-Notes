#include <bits/stdc++.h>

using namespace std;

int main() {
    freopen("data.txt", "rb", stdin);
    ios::sync_with_stdio(0), cin.tie(0);
    decltype(mt19937_64()()) tmp;
    while (scanf("%lu", &tmp) != EOF);
}