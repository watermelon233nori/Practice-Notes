#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    using ul = size_t;
    ul t;
    cin >> t;
    while (t--) {
        ul a, b;
        cin >> a >> b;
        ul op = 0, x;
    }
}