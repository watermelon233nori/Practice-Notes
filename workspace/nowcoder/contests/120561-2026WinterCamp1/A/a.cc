#include <bits/stdc++.h>

using namespace std;

constexpr auto MOD = 998244353;
int status[10] = {7, 2, 5, 5, 4, 5, 6, 3, 7, 6};

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    int t;
    cin >> t;
    while (t--) {
        int c;
        cin >> c;
        string s = to_string(c);
        int parr[7];
        for (int i = 0; i < 7; i++) cin >> parr[i];

    }
}