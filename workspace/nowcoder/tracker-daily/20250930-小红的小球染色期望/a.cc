#include <bits/stdc++.h>

using namespace std;

const int mod = 20010905;
const string str = "iloveyou";

int main() {
    ios::sync_with_stdio(0);
    cin.tie(nullptr);
    string str;
    getline(cin, str);
    for (auto& i : str) i = tolower(i);
    
    return 0;
}