#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    vector<vector<string>> partition(string s) {
        vector<vector<string>> ret;
        ret.emplace_back(vector<string>());
        return ret;
    }
};

int main() {
    string a = "";
    cout << a.length() << endl;
    return 0;
}