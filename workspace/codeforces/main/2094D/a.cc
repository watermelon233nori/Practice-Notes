#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    int t;
    cin >> t;
    string p, s;
    while (t--) {
        cin >> p >> s;
        ll pidx{}, sidx{};
        auto plen = p.size(), slen = s.size();
        bool legal = true;
        char pcur = p[0], scur = s[0];
        if (pcur != scur) {
        }
    }
}