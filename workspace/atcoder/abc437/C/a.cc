#include <bits/stdc++.h>

using namespace std;

struct deer {
    int w;
    int p;
};

bool cmp(const deer& l, const deer& r) {
    
}

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    int t;
    cin >> t;
    while (t--) {
        int n;
        cin >> n;
        vector<deer> d(n);
        for (int i = 0; i < n; i++) {
            cin >> d[i].w >> d[i].p;
        }


    }
}