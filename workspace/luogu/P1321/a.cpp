#include <bits/stdc++.h>

using namespace std;

string boy = "boy";
string girl = "girl";

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string str;
    str.reserve(300);
    cin >> str;
    for (const auto& i: str) {
        
    }
    return 0;
}