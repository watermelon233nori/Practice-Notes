#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    string longestPalindrome(string s) {
        stack<char> strStack;
        for (auto i: s) {
            if (strStack.top() ){}
        }
    }
};

int main() {
    Solution loc;
    stack<int> stk;
    cout << stk.top() << endl;
    return 0;
}