#include <bits/stdc++.h>

using namespace std;
using ll = long long;
using ull = unsigned ll;

static inline void solve() {
    
}

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
}