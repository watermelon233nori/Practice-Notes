#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    cout << fixed << setprecision(3);
    int r;
    cin >> r;
    cout << r * r * 3.14 << '\n';
}