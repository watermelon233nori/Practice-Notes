#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    int h, w, q;
    cin >> h >> w >> q;
    while (q--) {
        int t, val;
        cin >> t >> val;
        
    }
}