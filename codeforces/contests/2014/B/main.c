#include <stdio.h>


int main() {
    int t;
    scanf("%d", &t);
    for (int i = 0; i < t; ++i) {
        long n, k, leaf = 1;
        scanf("%ld%ld", &n, &k);
        if (n==1) {
            printf("NO\n");
            continue;
        }
    }
    return 0;
}