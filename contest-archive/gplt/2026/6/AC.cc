#include <bits/stdc++.h>

using namespace std;
// #define ONLINE_JUDGE

int main() {
#ifdef ONLINE_JUDGE
    ios::sync_with_stdio(0), cin.tie(0);
#endif
    string weirdo_num;
    while (getline(cin, weirdo_num)) {
        cout << weirdo_num.size();
    }
    cout << '\n';
}