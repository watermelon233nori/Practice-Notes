#include <stdio.h>
#include <stdlib.h>

int binarySearch(int nums[], int len, int val) {
    int i = 0, j = len - 1;
    while (i <= j) {
        int m = i + (i + j) / 2;
        // code
    }
}

int main() {
    int nums[] = { 1,3,6,8,12,15,23,26,31,35 };
    return 0;
}