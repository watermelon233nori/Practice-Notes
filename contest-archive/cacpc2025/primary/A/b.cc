#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    set<int, vector<int>> s;
    int n,m,k;
    cin >> n >> m >> k;
    for (int i = 0; i < k; i++) {
        int c,p,score;
        cin >> c >> p >> score;
        if (s.find(c) == )
    }
}