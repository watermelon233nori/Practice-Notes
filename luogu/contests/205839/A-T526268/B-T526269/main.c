#include <stdio.h>

int main() {
    int t;
    for (int i = 0; i < t; ++i) {
        int n;
        scanf("%d",&n);
        int arr[n];
        for (int i = 0; i<n; ++i) {
            scanf("%d",&arr[i]);
        }
        
    }
    return 0;
}
