#include <bits/stdc++.h>
#define IOS do{ios::sync_with_stdio(false);cin.tie(nullptr);}while(0);

using namespace std;

bool block[21][21][21];

int main() {
    IOS;
    int w, x, h, q;
    cin >> w >> x >> h >> q;
    
    return 0;
}