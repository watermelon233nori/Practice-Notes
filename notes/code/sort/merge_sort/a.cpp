#include <bits/stdc++.h>
#include "../../utils/print.hh" 

using namespace std;

void merge(vector<int>& nums) {}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    vector<int> vec;
    return 0;
}