#include <bits/stdc++.h>

using namespace std;
constexpr size_t CONST = 3.156e7;

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    size_t age;
    cin >> age;
    cout << CONST * age << '\n';
}