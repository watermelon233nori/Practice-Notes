#include <bits/stdc++.h>

using namespace std;

void times(string& a, string& b, string& c) {
    
    return;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string a, b, c;
    a.reserve(2020);
    b.reserve(2020);
    cin>>a>>b;
    c.reserve(4010);
    return 0;
}